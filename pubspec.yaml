name: meteo_pocket
description: "Météo Pocket - application météo Flutter avec Riverpod et Open-Meteo."
publish_to: 'none'
version: 4.0.0+4

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.4.3
  http: ^1.6.0
  intl: ^0.20.3
  shared_preferences: ^2.5.5
  geolocator: ^14.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
